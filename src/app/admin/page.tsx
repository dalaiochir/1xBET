import { getServerSession } from 'next-auth';import { authOptions } from '@/lib/auth';import { prisma } from '@/lib/prisma';
export default async function Admin(){const s=await getServerSession(authOptions);if(!s?.user||(s.user as any).role!=='ADMIN')return <><h1>Admin</h1><p>Admin access required.</p></>;const matches=await prisma.match.findMany({include:{teamA:true,teamB:true,markets:{include:{outcomes:true}}},take:30});return <><h1>Admin dashboard</h1><div className="grid"><div className="card"><h3>Create/Edit/Delete matches</h3><p className="muted">Use admin API endpoints for production-safe operations.</p></div><div className="card"><h3>Markets</h3><p className="muted">Lock, unlock, set winners, and settle predictions through API.</p></div><div className="card"><h3>Demo balances</h3><p className="muted">Adjust via Prisma/admin actions with transaction records.</p></div></div><h2>Recent matches</h2><table className="table"><tbody>{matches.map(m=><tr key={m.id}><td>{m.teamA.name} vs {m.teamB.name}</td><td>{m.status}</td><td>{m.markets.length} markets</td></tr>)}</tbody></table></>}
