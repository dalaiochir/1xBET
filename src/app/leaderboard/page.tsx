import { prisma } from '@/lib/prisma';
export default async function Leaderboard(){const users=await prisma.user.findMany({include:{predictions:true},orderBy:{demoBalance:'desc'},take:50});return <><h1>Leaderboard</h1><table className="table"><thead><tr><th>Rank</th><th>Username</th><th>Profit</th><th>Win rate</th><th>Total predictions</th></tr></thead><tbody>{users.map((u,i)=>{const won=u.predictions.filter(p=>p.status==='WON').length;const wr=u.predictions.length?Math.round(won/u.predictions.length*100):0;return <tr key={u.id}><td>{i+1}</td><td>{u.username}</td><td>{u.demoBalance-10000}</td><td>{wr}%</td><td>{u.predictions.length}</td></tr>})}</tbody></table></>}
