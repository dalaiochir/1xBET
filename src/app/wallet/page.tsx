import { getServerSession } from 'next-auth';import { authOptions } from '@/lib/auth';import { prisma } from '@/lib/prisma';import { Disclaimer } from '@/components/Disclaimer';
export default async function Wallet(){const s=await getServerSession(authOptions);if(!s?.user)return <><h1>Wallet</h1><p>Please login.</p></>;const user=await prisma.user.findUnique({where:{id:(s.user as any).id},include:{transactions:{orderBy:{createdAt:'desc'},take:25}}});return <><h1>Wallet</h1><Disclaimer/><div className="card"><p className="muted">Current balance</p><h2>{user?.demoBalance ?? 0} demo coins</h2><p className="muted">This balance has no real-world value.</p><form action="/api/wallet/daily-claim" method="post"><button className="btn">Claim 500 daily demo coins</button></form></div><h2>Transaction history</h2><table className="table"><tbody>{user?.transactions.map(t=><tr key={t.id}><td>{t.type}</td><td>{t.amount}</td><td>{t.balanceAfter}</td><td>{new Date(t.createdAt).toLocaleString()}</td></tr>)}</tbody></table></>}
