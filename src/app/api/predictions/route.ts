import { getServerSession } from 'next-auth';import { redirect } from 'next/navigation';import { authOptions } from '@/lib/auth';import { prisma } from '@/lib/prisma';import { bad, ok } from '@/lib/response';import { predictionSchema } from '@/lib/validation';
export async function POST(req:Request){const session=await getServerSession(authOptions);if(!session?.user)return bad('Login required',401);const ct=req.headers.get('content-type')||'';let raw:any;if(ct.includes('form')){const f=await req.formData();raw={matchId:f.get('matchId'),marketId:f.get('marketId'),outcomeId:f.get('outcomeId'),stakeAmount:Number(f.get('stakeAmount'))}}else raw=await req.json();const parsed=predictionSchema.safeParse(raw);if(!parsed.success)return bad('Invalid prediction');const userId=(session.user as any).id;const result=await prisma.$transaction(async tx=>{const user=await tx.user.findUnique({where:{id:userId}});if(!user)throw new Error('No user');const market=await tx.market.findUnique({where:{id:parsed.data.marketId},include:{match:true,outcomes:true}});if(!market||market.matchId!==parsed.data.matchId)throw new Error('Invalid market');if(market.isLocked||market.match.status==='FINISHED')throw new Error('Market unavailable');const outcome=market.outcomes.find(o=>o.id===parsed.data.outcomeId);if(!outcome)throw new Error('Invalid outcome');if(parsed.data.stakeAmount>user.demoBalance)throw new Error('Insufficient balance');const balanceAfter=user.demoBalance-parsed.data.stakeAmount;await tx.user.update({where:{id:userId},data:{demoBalance:balanceAfter}});await tx.demoTransaction.create({data:{userId,type:'PREDICTION_STAKE',amount:-parsed.data.stakeAmount,balanceAfter}});return tx.prediction.create({data:{userId,matchId:parsed.data.matchId,marketId:parsed.data.marketId,outcomeId:parsed.data.outcomeId,stakeAmount:parsed.data.stakeAmount,potentialReturn:Math.floor(parsed.data.stakeAmount*outcome.multiplier)}})}).catch(e=>({error:e.message}));if('error' in result)return bad(result.error);if(ct.includes('form'))redirect('/profile');return ok(result)}
