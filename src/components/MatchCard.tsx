import Link from 'next/link';
export function MatchCard({match}:{match:any}){return <div className="card"><div className="row"><span className={`status ${match.status}`}>{match.status}</span><span className="muted">{match.format}</span></div><h3>{match.teamA?.name ?? match.teamAId} vs {match.teamB?.name ?? match.teamBId}</h3><p className="muted">{match.tournament?.name ?? match.tournamentId} • {match.game?.name ?? match.gameId}</p><p>{new Date(match.startTime).toLocaleString()}</p><div className="row"><span>Demo multiplier 1.85x</span><Link className="btn" href={`/matches/${match.id}`}>Predict</Link></div></div>}
